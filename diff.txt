diff --git a/src/components/admin/SentimentCharts.jsx b/src/components/admin/SentimentCharts.jsx
index 89adf31..a46dab5 100644
--- a/src/components/admin/SentimentCharts.jsx
+++ b/src/components/admin/SentimentCharts.jsx
@@ -39,12 +39,12 @@ const SentimentCharts = ({ feedbackList = [] }) => {
     if (pieData.length === 0) return null;
 
     return (
-        <div className={`grid grid-cols-1 md:grid-cols-2 gap-6 mb-8 ${shouldAnimate ? 'animate-in fade-in duration-500' : ''}`}>
+        <div className={`flex flex-col gap-6 h-full ${shouldAnimate ? 'animate-in fade-in duration-500' : ''}`}>
 
             {/* Pie Chart: Overall Sentiment */}
-            <div className="bg-white/50 backdrop-blur-xl border border-white/60 rounded-3xl p-6 shadow-sm">
+            <div className="bg-white/50 backdrop-blur-xl border border-white/60 rounded-3xl p-6 shadow-sm flex-1 min-h-[300px]">
                 <h3 className="font-bold text-gray-800 mb-4">Overall Sentiment</h3>
-                <div className="h-[250px] w-full">
+                <div className="h-[220px] w-full">
                     <ResponsiveContainer width="100%" height="100%">
                         <PieChart>
                             <Pie
@@ -70,9 +70,9 @@ const SentimentCharts = ({ feedbackList = [] }) => {
             </div>
 
             {/* Bar Chart: Sentiment by Page */}
-            <div className="bg-white/50 backdrop-blur-xl border border-white/60 rounded-3xl p-6 shadow-sm">
+            <div className="bg-white/50 backdrop-blur-xl border border-white/60 rounded-3xl p-6 shadow-sm flex-1 min-h-[300px]">
                 <h3 className="font-bold text-gray-800 mb-4">Sentiment by Page</h3>
-                <div className="h-[250px] w-full">
+                <div className="h-[220px] w-full">
                     <ResponsiveContainer width="100%" height="100%">
                         <BarChart
                             data={barData}
diff --git a/src/context/CartContext.jsx b/src/context/CartContext.jsx
index aae1ab0..4f8210a 100644
--- a/src/context/CartContext.jsx
+++ b/src/context/CartContext.jsx
@@ -99,7 +99,6 @@ export const CartProvider = ({ children }) => {
         updateQuantity,
         clearCart,
         cartCount,
-        cartCount,
         cartTotal,
         getCartTotal: () => cartTotal
     }), [cartItems, cartCount, cartTotal]);
diff --git a/src/context/LoyaltyContext.jsx b/src/context/LoyaltyContext.jsx
index 37333d8..6b9db7a 100644
--- a/src/context/LoyaltyContext.jsx
+++ b/src/context/LoyaltyContext.jsx
@@ -14,6 +14,7 @@ export const LoyaltyProvider = ({ children }) => {
     const [tier, setTier] = useState('Silver'); // Silver, Gold, Platinum
     const [history, setHistory] = useState([]);
     const [streak, setStreak] = useState(0);
+    const [highestStreak, setHighestStreak] = useState(0);
     const [lastClaimDate, setLastClaimDate] = useState(null);
     const [loading, setLoading] = useState(true);
 
@@ -37,6 +38,7 @@ export const LoyaltyProvider = ({ children }) => {
                 setCoins(data.coins || 0);
                 setHistory(data.loyaltyHistory || []);
                 setStreak(data.currentStreak || 0);
+                setHighestStreak(data.highestStreak || 0);
                 setLastClaimDate(data.lastClaimDate || null);
 
                 // Tier logic based on simple thresholds
@@ -60,6 +62,7 @@ export const LoyaltyProvider = ({ children }) => {
             setTier('Silver');
             setHistory([]);
             setStreak(0);
+            setHighestStreak(0);
             setLastClaimDate(null);
             setLoading(false);
         });
@@ -155,13 +158,13 @@ export const LoyaltyProvider = ({ children }) => {
             const rewardAmount = getRewardForStreak(newStreak);
             const tierReached = getCurrentTier(newStreak);
 
-            const userRef = doc(db, 'users', currentUser.uid);
+            const newHighestStreak = Math.max(highestStreak, newStreak);
 
             await updateDoc(userRef, {
                 currentStreak: newStreak,
                 lastClaimDate: today,
                 totalRewardsClaimed: increment(1),
-                highestStreak: increment(newStreak > (userRef.highestStreak || 0) ? 1 : 0), // Note: Firestore can't read-mod-write atomic easily like this for max, but sufficient for now
+                highestStreak: newHighestStreak,
                 tierReached: tierReached.name
             });
 
diff --git a/src/context/NotificationContext.jsx b/src/context/NotificationContext.jsx
index 80eae7a..1410fb1 100644
--- a/src/context/NotificationContext.jsx
+++ b/src/context/NotificationContext.jsx
@@ -300,6 +300,7 @@ export const NotificationProvider = ({ children }) => {
     const triggerAbandonedCart = async () => {
         if (!currentUser) return;
         try {
+            const trigger = httpsCallable(functions, 'triggerAbandonedCart');
             await trigger();
             // addNotification('announcement', 'Abandoned cart check triggered!', true); 
             // Don't show toast, let the real push come through if meaningful
diff --git a/src/pages/Account.jsx b/src/pages/Account.jsx
index 110b511..3e682f8 100644
--- a/src/pages/Account.jsx
+++ b/src/pages/Account.jsx
@@ -154,6 +154,15 @@ const Account = () => {
                                         placeholder="+91 98765 43210"
                                     />
                                 </div>
+                                <div>
+                                    <label className="text-xs font-bold text-gray-500 uppercase">Address</label>
+                                    <textarea
+                                        value={formData.address || ''}
+                                        onChange={e => setFormData({ ...formData, address: e.target.value })}
+                                        className="w-full mt-1 p-3 bg-slate-50 rounded-xl font-bold text-slate-900 border-transparent focus:border-orange-500 focus:ring-0 outline-none transition-all resize-none min-h-[80px]"
+                                        placeholder="Enter your complete delivery address"
+                                    />
+                                </div>
                                 <div className="flex gap-2 pt-2">
                                     <Button
                                         type="button"
